using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FollowPlayer : MonoBehaviour
{
    public Transform playerTransform;

    void Update()
    {

        //this.transform.position = Vector2.Lerp(this.transform.position, new Vector2(playerTransform.position.x, this.transform.position.y), 99999f);
    }
}
